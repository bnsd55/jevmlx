---
name: Bug report
about: Something crashed, hung, or produced wrong output
labels:
  - bug
---

**jevmlx version** (output of `jevmlx --version`):

**macOS version + chip** (e.g. "macOS 15.2, M2 Pro"):

**Model id** (e.g. `mlx-community/Qwen2.5-1.5B-Instruct-4bit`):

**Environment check** (paste output of `jevmlx doctor --json`):

```
(paste doctor output here)
```

**Command run**:

**Expected**:

**Actual** (paste output):

```
(paste output here)
```
